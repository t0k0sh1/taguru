; Pack-copy the file: SDK dependencies instead of symlinking them. A symlink
; resolves modules from its real path, so langchain-taguru would pick up the
; @langchain/core under sdk/node_modules — a second copy next to the one
; installed here. One tree, one @langchain/core.
install-links=true
